Change default app menus (top menu - file, help..)

Find a way to "save" ssh sessions when user uses ssh

Open session file when program starts, then read session file and display on index.html

Add support for Dark Mode (like in macOS) (For now it's light force)

Support "folders" in bookmarks and also recursive folders

In electron tabs - on selected tab - you can see next to the 'X' button to close it, you can see '-'. In HTML element inspector it's the content for '::after' . Need to fix this.